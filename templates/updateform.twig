<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Article</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
</head>
<body class="flex justify-center items-center h-screen bg-gray-100">
<div class="text-center max-w-6xl px-4">
    <h1 class="text-3xl font-bold mb-6 mt-8">Edit Article</h1>

    {% if error %}
        <div class="error">{{ error }}</div>
    {% endif %}

    <form action="/article/{{ article.id }}/edit" method="POST" class="bg-white p-6 rounded-lg shadow-md max-w-full mx-auto">
        <div class="mb-4">
            <label for="author" class="block text-gray-600">Author:</label>
            <input type="text" id="author" name="author" value="{{ article.author }}" required class="w-full md:w-96 px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500">
        </div>
        <div class="mb-4">
            <label for="title" class="block text-gray-600">Title:</label>
            <input type="text" id="title" name="title" value="{{ article.title }}" required class="w-full md:w-96 px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500">
        </div>
        <div class="mb-6">
            <label for="content" class="block text-gray-600">Content:</label>
            <textarea id="content" name="content" rows="8" required class="w-full md:w-96 px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-blue-500">{{ article.content }}</textarea>
        </div>
        <div>
            <button class="w-full px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600" type="submit">Update Article</button>
        </div>
    </form>

    <a class="block mt-4 px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600" href="/articles">Back to articles list</a>
</div>
</body>
</html>